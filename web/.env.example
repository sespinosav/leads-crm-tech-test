# When the backend has API_KEY set, mirror it here so the dashboard can call
# the protected endpoints. Leave empty to skip the header.
VITE_API_KEY=

# Optional — point the built bundle at a remote API origin. Empty means
# "same origin / use Vite dev proxy".
VITE_API_BASE=
